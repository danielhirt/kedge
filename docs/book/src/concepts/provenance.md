# Provenance

Content-addressed provenance (`sig:` prefix) vs legacy git SHA provenance. How fingerprints survive rebase, amend, and squash. Stamping with `kedge link` and advancing with `kedge sync`.
